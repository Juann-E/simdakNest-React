1. 
    ALTER TABLE barang_pasar_grid
    ADD COLUMN keterangan TEXT NULL AFTER id_barang;
2. 
    ALTER TABLE harga_barang_pasar
    ADD COLUMN keterangan TEXT AFTER harga;
3.  barang grid full main di body json untuk milih nya
---- ini baru ----
4. jalankan ulang (hapus table lama dulu baru jalankan ini)
    CREATE TABLE harga_barang_pasar (
    id_harga INT AUTO_INCREMENT PRIMARY KEY,
    id_barang_pasar INT NOT NULL,
    harga DECIMAL(15,2) NOT NULL,
    keterangan TEXT,
    time_stamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_barang_pasar) REFERENCES barang_pasar_grid(id_barang_pasar)
        ON UPDATE CASCADE
        ON DELETE CASCADE
);